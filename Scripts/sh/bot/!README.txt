------!!!------
DO NOT USE PackAllLinux and PackAllLinuxBot!!!
------!!!------

buildAllBotRelease
    Builds all projects + bot with release configuration
buildAllBotDebug
    Builds all projects + bot with debug configuration
buildAllBotTools
    Builds all projects + bot with tools configuration
runQuickAllBot
    Runs the client and server + bot without building
runQuickServerBot
    Runs the server + bot without building
